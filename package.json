{
  "name": "kadupul-website",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/build-map.mjs && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint:prose": "node scripts/lint-prose.mjs",
    "check:all": "npm run lint:prose && npm run build && npm run check:links",
    "build:map": "node scripts/build-map.mjs",
    "check:links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "@astrojs/starlight": "^0.42.0",
    "astro": "^7.3.2",
    "sharp": "^0.35.4",
    "starlight-versions": "^0.10.1"
  }
}
